/* ============================================================
   CONFIG — Edit this file to match your local environment.
   This is the single source of truth for the API base URL.
   ============================================================ */
const API_BASE = 'http://localhost:3000';
